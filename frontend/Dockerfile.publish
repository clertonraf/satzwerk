# Lightweight runtime image — expects pre-built static assets at dist/
# Used by the CI publish job, which uploads dist/ as an artifact from the
# build job and downloads it here so Node/npm never runs inside Docker.
FROM nginx:alpine AS runtime
COPY dist/ /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80

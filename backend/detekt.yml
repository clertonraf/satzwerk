build:
  maxIssues: 0

config:
  validation: true

processors:
  active: true

console-reports:
  active: true

exceptions:
  TooGenericExceptionCaught:
    excludes:
      - '**/common/HandlerSupport.kt'
